/** Frame-driven primitives only. Scene composition and geometry are authored per script. */
export { AbsoluteFill, interpolate, spring, useCurrentFrame, useVideoConfig, Sequence, Easing } from 'remotion';
export { ThreeCanvas } from '@remotion/three';
export type { ThreeElements } from '@react-three/fiber';
